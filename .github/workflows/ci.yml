name: CI

on:
  push:
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo, pdo_pgsql, intl, mbstring, zip, xml, dom
          coverage: none

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: Install Symfony CLI
        run: |
          curl -1sLf 'https://dl.cloudsmith.io/public/symfony/stable/setup.deb.sh' | sudo -E bash
          sudo apt install symfony-cli

      - name: Set up Docker Compose
        id: docker-compose
        run: |
          # Vérifier si docker-compose est disponible, sinon utiliser docker compose
          if command -v docker-compose &> /dev/null; then
            echo "cmd=docker-compose" >> $GITHUB_OUTPUT
          else
            echo "cmd=docker compose" >> $GITHUB_OUTPUT
          fi

      - name: Start Docker Compose services
        run: ${{ steps.docker-compose.outputs.cmd }} up -d

      - name: Wait for database to be ready
        run: |
          DOCKER_CMD="${{ steps.docker-compose.outputs.cmd }}"
          until $DOCKER_CMD exec -T database pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB:-app}; do
            echo "Waiting for database..."
            sleep 2
          done

      - name: Wait for FusionAuth to be ready
        run: |
          DOCKER_CMD="${{ steps.docker-compose.outputs.cmd }}"
          echo "Waiting for FusionAuth to be ready..."
          max_attempts=60
          attempt=0
          while [ $attempt -lt $max_attempts ]; do
            if curl -f -s http://localhost:9011/api/status > /dev/null 2>&1; then
              echo "FusionAuth is ready!"
              break
            fi
            echo "Waiting for FusionAuth... (attempt $((attempt+1))/$max_attempts)"
            sleep 5
            attempt=$((attempt+1))
          done
          if [ $attempt -eq $max_attempts ]; then
            echo "FusionAuth failed to start after $max_attempts attempts"
            exit 1
          fi

      - name: Composer security audit
        run: composer audit

      # Installation de jq pour parser les réponses JSON d'Ollama
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      # Installation d'Ollama pour l'analyse SOLID
      - name: Setup Ollama
        uses: ai-action/setup-ollama@v1

      # Cache des modèles Ollama pour accélérer les runs
      - name: Cache Ollama models
        uses: actions/cache@v4
        with:
          path: ~/.ollama
          key: ${{ runner.os }}-ollama-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-ollama-

      # Télécharger le modèle LLM (llama3.2 est un bon compromis vitesse/qualité)
      - name: Pull LLM model
        run: |
          ollama pull llama3.2

      # Analyse SOLID avec Ollama sur les fichiers PHP modifiés
      - name: SOLID check with Ollama
        id: solid-check
        continue-on-error: true
        run: |
          # Déterminer la référence de base selon le contexte
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE_REF="${{ github.event.pull_request.base.sha }}"
            HEAD_REF="${{ github.event.pull_request.head.sha }}"
          else
            # Pour les pushes, comparer avec le commit précédent
            BASE_REF="HEAD^"
            HEAD_REF="HEAD"
          fi

          # Exécuter l'analyse SOLID
          ./.github/scripts/check-solid.sh llama3.2 "$BASE_REF" "$HEAD_REF" || true
          
          # Récupérer le chemin du rapport généré
          REPORT_FILE="${GITHUB_WORKSPACE}/.github/solid-reports/solid-report.md"
          if [ -f "$REPORT_FILE" ]; then
            echo "report_file=$REPORT_FILE" >> $GITHUB_OUTPUT
            echo "report_exists=true" >> $GITHUB_OUTPUT
            echo ""
            echo "📋 Rapport SOLID généré:"
            cat "$REPORT_FILE"
          else
            echo "report_exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ Aucun rapport généré (peut-être aucun fichier PHP modifié)"
          fi

      # Poster un commentaire sur la PR avec le rapport SOLID
      - name: Comment PR with SOLID report
        if: github.event_name == 'pull_request' && steps.solid-check.outputs.report_exists == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const reportFile = '${{ steps.solid-check.outputs.report_file }}';
            
            let report = '## 🔍 Analyse SOLID\n\n';
            report += 'Analyse automatique des principes SOLID effectuée sur les fichiers PHP modifiés.\n\n';
            report += '---\n\n';
            
            try {
              if (fs.existsSync(reportFile)) {
                const content = fs.readFileSync(reportFile, 'utf8');
                report += content;
              } else {
                report += '⚠️ Impossible de récupérer le rapport complet. Consultez les logs de la CI pour plus de détails.';
              }
            } catch (error) {
              report += `⚠️ Erreur lors de la lecture du rapport: ${error.message}`;
            }
            
            // Chercher un commentaire existant de cette action
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('🔍 Analyse SOLID')
            );
            
            if (botComment) {
              // Mettre à jour le commentaire existant
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: report
              });
            } else {
              // Créer un nouveau commentaire
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: report
              });
            }

      - name: Wait before running tests
        run: sleep 60

      - name: Run tests
        run: make tests

      - name: Stop Docker Compose services
        if: always()
        run: ${{ steps.docker-compose.outputs.cmd }} down -v

